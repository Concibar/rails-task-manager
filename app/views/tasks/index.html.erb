<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= task["completed"] %> <%= link_to task_path(task) do %> <%= task["title"] %> <% end %> </li>
  <% end %>
</ul>

<%= link_to "Create a new task", "/tasks/new" %>
